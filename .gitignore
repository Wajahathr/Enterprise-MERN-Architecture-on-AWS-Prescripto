# Node modules
node_modules/

# Environment variables (NEVER COMMIT THESE)
.env
.env.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Lock files (Optional, but good practice if you want clean repos)
package-lock.json

# AWS/Discord Scripts (Agar laptop par rakhi hain toh ignore karein)
monitor.sh
backup.sh